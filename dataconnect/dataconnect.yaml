specVersion: "v1"
serviceId: "thangamalar"
location: "us-central1"
schema:
  source: "./schema"
  datasource:
    postgresql:
      database: "fdcdb"
      cloudSql:
        instanceId: "thangamalar-fdc"
      # schemaValidation: "COMPATIBLE"
connectorDirs: ["./connector"]
